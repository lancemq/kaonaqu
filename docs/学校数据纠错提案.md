# 学校数据纠错提案（基于微信公众号文章核验）

> 状态：**待确认，未改动任何数据**。本文仅列出拟修改项与证据，等你确认后再动手。
> 生成时间：2026-07-08

## 一、核验方法

- **语料来源**：通过微信搜狗检索「上海 四校八大 / 市重点 / 市特色普通高中 / 各区学校」等关键词，共采集 **87 + 追加 18 篇** 公众号文章（标题+摘要），信源含官方/权威号：**上海普陀（普陀区官号）、上海教育、第一教育、上海市曹杨中学（学校官号）、上海升学、上哪学、今日闵行、学生导报** 等。
- **匹配方式**：按校名在 `data/schools.json`（911 条）中**精确匹配**，仅当满足以下任一条件才提案纠错：
  - ≥ 2 个**相互独立**信源明确称该校为「上海市特色普通高中」；或
  - 1 个**官方/权威**信源（区官号、学校官号、市教委关联媒体）明确确认。
- **噪声过滤**：上一轮分析发现语料摘要存在明显错误标签（如「上海中学=区重点/民办」「四校=八大」），此类**不予采纳**，原文数据保留。

## 二、拟纠错清单（高置信度，建议采纳）

| # | 学校 | 区 | 当前 `schoolKeyLevel` | 拟改为 | 当前学段 | 拟改学段 | 证据信源 |
|---|------|----|------|------|------|------|------|
| 1 | 上海市曹杨中学 | 普陀 | `区重点(高中)` | `市特色普通高中(高中)` | 高中 | 高中（不变） | 上海市曹杨中学官号「全市首家特色普通高中」；上海升学「2017年被命名为上海市第一所特色普通高中」；上海普陀；华师教辅ECNUP（列入12所名单） |
| 2 | 上海市甘泉外国语中学 | 普陀 | `区重点(高中)` | `市特色普通高中(高中)` | 完全中学 | 完全中学（不变） | 上海普陀（官号）「甘泉外国语中学在名单之列，继曹杨中学获首批」；华师教辅ECNUP列入名单；上海升学2018自招计划列其45名 |
| 3 | 上海市闵行第三中学 | 闵行 | `一般高中` | `市特色普通高中(高中)` | 高中 | 高中（不变） | 上哪学、今日闵行 同载「拟命名上海市特色普通高中学校名单…闵行区 上海市闵行第三中学」 |
| 4 | 香山中学 | 浦东 | `一般初中` | `市特色普通高中(高中)` | **初中** | **高中** ⚠️ | 上哪学、今日闵行 同载「…浦东新区 上海市香山中学」；该校为美术特色高中，当前被错归为初中 |
| 5 | 上海市崇明区城桥中学 | 崇明 | `市重点(高中)` | `市特色普通高中(高中)` | 高中 | 高中（不变） | 上哪学、今日闵行 同载「…崇明区 上海市崇明区城桥中学」；城桥为特色普通高中（非市实验性示范性），原「市重点」不够准确 |

**说明**：
- 这 5 所均为「上海市特色普通高中」（市教委命名，享市重点同等待遇、可全市自招），属官方确切称号，纠错风险低。
- 第 4 项（香山中学）除层级外，**学段也错**（初中→高中），为较重要修正。

## 三、需要你拍板的两个问题

### 问题 A：`schoolKeyLevel` 取值
当前枚举中**没有**「市特色普通高中(高中)」这个值（现有值：市重点/区重点/一般高中 + 初中类）。拟**新增**该取值。
- 若你更希望**保留原有基础层级**（如曹杨/甘泉保留「区重点」），另用其他字段标记「特色普通高中」，请告知——那需要改数据模型（加字段）。
- 若接受新增枚举值，请确认；同时需注意前端筛选/分类逻辑是否依赖硬编码的「市重点(高中)」，可能需要把「市特色普通高中(高中)」归并到市重点分组展示。

### 问题 B：修改目标
- 运行时库是 **Supabase**（`schools` 表在线 904 行），本地 `data/schools.json` 为 911 条，两者**已存在 7 条差异**。
- 建议做法：先改**本地 `data/schools.json`**（站点内容源），再按需 `npm run data:sync:supabase` 同步到线上。
- 请确认：① 仅改本地文件？② 还是本地+同步 Supabase？③ 是否先做一次本地↔线上对账再改？

## 四、明确不改的项（噪声/存疑，已在分析中被排除）

- **上海中学 / 华二 / 复旦附中 / 交大附中（四校）**：语料中偶有「区重点/八大」等错误摘要，我校数据（`市重点(高中)` + `四校`）正确，**不改**。
- **曹杨第二中学、华二紫竹等**：我校已正确标为「市重点/四校分校」，语料无矛盾，**不改**。
- 其余 900+ 所学校：本次未做全量核验，**不在本次改动范围**。

## 五、可选后续（未执行，等你指示）

1. **扩展核验**：上海累计已命名 **19 所**市特色普通高中（如华东政法附中、北蔡、东昌、上戏附中、同济二附中、莘庄、松江一中、徐汇中学等），可对照我校数据批量核对是否还有同类错标。
2. **缺失字段补充**：部分学校缺 `address`/`foundingYear` 等，可由语料中明确的「创办于19XX年」「地址：…」句补充（需逐条人工复核，风险较高，建议单独走流程）。

---
**下一步**：请回复确认 **问题 A / 问题 B** 的选择，我将据此执行修改（默认方案：新增枚举值 + 仅改本地 `data/schools.json` 的上述 5 所）。
